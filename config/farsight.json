{
  "maxRenderDistance": {
    // Maximum allowed render distance, changes require a game restart. 
    // Default 64
    "maxRenderDistance": 64
  },

  "chunkPreview": {
    "descEnable:": "Enables chunk preview feature, which enables saving and loading previously visited chunks to/from disk, default true",

    "enabled": true,

    "descSpeed:": "Set the speed at which chunk preview loads chunks from disk, default and minimum 1 Chunk per tick(20 per second)",

    "previewChunkLoadSpeed": 1
  },

  "debugLogging": {
    // Enables debug logging to latest.log, 
    // Default false
    "debugLogging": false
  }
}
